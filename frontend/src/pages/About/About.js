import React from "react";
import { useStyles } from "./AboutStyles";

const About = () => {
  const styles = useStyles();

  return (
    <div>
      <h1>About</h1>
    </div>
  );
};

export default About;
