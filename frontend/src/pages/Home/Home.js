import React from "react";
import { useStyles } from "./HomeStyles";

const Home = () => {
  const styles = useStyles();

  return (
    <div>
      <h1>Home</h1>
    </div>
  );
};

export default Home;
